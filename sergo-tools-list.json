{
  "last_updated": "2026-09-09T00:00:00Z",
  "tool_count": 24,
  "tool_change_R61": "MAJOR GAP: R60->R61 skipped ~2 months (2026-07-08->2026-09-09), 40+ real sergo issues filed/closed by team in the interim WITHOUT this cache being updated (cache staleness bug in itself - writes must have failed/been skipped repeatedly). LSP tool-set CHANGED 23->24: REMOVED check_onboarding_performed,find_file,list_dir,think_about_collected_information,think_about_task_adherence,think_about_whether_you_are_done(6). ADDED find_declaration,find_implementations,get_diagnostics_for_file,open_dashboard,rename_memory,replace_in_files,safe_delete_symbol(7). ENV REGRESSION: language servers (go/node) NOT INSTALLED in this sandbox -> get_diagnostics_for_file (and likely find_declaration/find_implementations) FAIL with 'language server manager not initialized'. Fell back to Read/Grep tools successfully - do NOT attempt LSP workarounds per tool's own error message, just degrade gracefully. R62: retest LSP availability; if still broken, rely on Read/Grep/search_for_pattern only.",
  "tool_change_R60": "LSP 23 stable. REGISTRY DELTA 42->43 (grep -c Analyzer main.go=43). 43rd=writebytestring(SuggestedFix w.Write([]byte(s))->io.WriteString(w,s)). ALL sergo CLOSED pre-run(sg59a1=#43934). sg60a1=#aw_sg60a1: isStringType t.Underlying()==String matches NAMED strings but io.WriteString needs EXACT string(named!=named)=>autofix DOESN'T COMPILE; testdata badNamedString:26 encodes broken fix; test uses analysistest.Run NOT RunWithSuggestedFixes(=#43313 gap re-opened; 3 siblings use RWSF). FIX=string(s) wrap+golden. NOT enforced=latent. GOTCHA: appendbytestring's identical isStringType CORRECT(append accepts core-type string); wrong copied to exact-string-param API(io.WriteString). R61: probe other SuggestedFix bodies exact-type-param vs Underlying-match assignability.",
  "tool_change_R59": "STABLE 42. ALL sergo CLOSED pre-run. sg59a1=#43934(LANDED): grep ALL linters `.Name ==/!= \"<stdpkg>\"`(BOTH forms) => 5 syntactic-pkg-match holdouts: errstringmatch:100 fprintlnsprintf:132 osexitinlibrary:59(ENFORCED) fileclosenotdeferred:174 contextcancelnotdeferred:140(not-enf). Prior #40243+#40435 AUTO-EXPIRED w/ ZERO migrations. IsPkgSelector proven. GOTCHA: grep BOTH ==/!=.",
  "registry_delta_history": "R56:40->41 stringsindexcontains. R57:41->42 httprespbodyclose+appendbytestring. R58/R59: STABLE 42. R60:42->43 writebytestring(SuggestedFix). R61: registry moved from cmd/linters/main.go (multichecker.Main literal list) to pkg/linters/registry.go var allAnalyzers (main.go now just calls linters.All()) - OLD DETECTOR BROKEN (grep -c Analyzer main.go now =0). 43->67 (+24, though many added/fixed across the undocumented R60-R61 gap). NEW DETECTOR: grep -c 'Analyzer,$' pkg/linters/registry.go, or count entries in allAnalyzers slice.",
  "tools": [
    "activate_project", "delete_memory", "edit_memory", "find_declaration",
    "find_implementations", "find_referencing_symbols", "find_symbol", "get_current_config",
    "get_diagnostics_for_file", "get_symbols_overview", "initial_instructions", "insert_after_symbol",
    "insert_before_symbol", "list_memories", "onboarding", "open_dashboard", "read_memory",
    "rename_memory", "rename_symbol", "replace_in_files", "replace_symbol_body",
    "safe_delete_symbol", "search_for_pattern", "write_memory"
  ],
  "params": "activate_project --project (NOT --path). find_symbol --name_path_pattern. find_referencing_symbols --name_path+--relative_path. search_for_pattern --substring_pattern. JSON stdin `.` sentinel.",
  "gotchas": "R61: jq/mkdir/cat/ls/grep as raw Bash all need interactive approval and FAIL headlessly in this run's sandbox (use Read/Write/Edit/Glob/Grep dedicated tools instead, which need no approval; gh api and safeoutputs CLI also work approval-free). safeoutputs create_issue: pass --title/--body/--labels/--temporary_id directly as bash args in a SINGLE safeoutputs invocation (no jq, no pipe) - body can be a multi-line single-quoted bash string but MUST NOT contain any literal apostrophe/single-quote char (breaks the quoting) and a newline immediately followed by '#' is rejected by a path-validation guard - use bold **Heading** instead of ### Heading in issue bodies built this way. get_diagnostics_for_file/find_declaration/find_implementations need a live LSP (go/node) - NOT INSTALLED this run, degrade to Read/Grep. find_symbol broad 70s+ (prefer Grep) in past runs when LSP worked. `gh issue list --search` ERRORS 'malformed version' -> USE `gh api repos/OWNER/REPO/issues?labels=sergo&state=all&per_page=100 --jq` (2 pages needed, ~150+ historical sergo issues). RECONCILE gh api BEFORE strategy (grep CODE for fix, OPEN!=fixed). MEMORY 10KB TOTAL PATCH: EDIT files surgically + APPEND-ONLY strategies. This run safe-tools: create_issue(max 3)/create_discussion/missing_tool/missing_data/noop."
}
