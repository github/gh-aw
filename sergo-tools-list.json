{
  "last_updated": "2026-09-16T00:00:00Z",
  "tool_count": 24,
  "tool_change_R69": "STABLE 24. Registry STABLE 71 (no delta since R68, grep -c 'Analyzer,\\$' registry.go=71). Reconcile: 59627 (sg61a1, 4-linter main-guard gap) flipped closed/not_planned since R68 - verified via direct Read/Grep of all 4 files (osexitinlibrary.go:23, rawloginlib.go:30, logfatallibrary.go:35, panic-in-library-code.go:36) that NONE gained the pass.Pkg.Name()==\"main\" check osgetenvlibrary.go:22 already has - THIRD confirmed reverse-phantom (after 58376/60557 and 59122/61023) - refiled as sg69a1. Other 6 pre-run opens (61265=sg68a1,61024=sg67a2,61023=sg67a1,60738=sg66a1,60367=sg64a1,60170=sg63a1,59875=sg62a1) unchanged/still open. gh api search/issues?q=repo:OWNER/REPO+TERM used to confirm nilctxpassed/UnwrapParenExpr had zero prior issue coverage before filing sg69a2. 2 issues this run. safeoutputs create_issue via direct --title/--body/--labels/--temporary_id bash flags with a single-quoted multi-paragraph body (bold headers, zero apostrophes, en-dash/double-hyphen instead of contractions) worked in ONE shot both times, no approval prompt, no retry.",
  "tool_change_R65": "STABLE 24 (no change from R64). activate_project OK (go/typescript/bash) as plain Bash call. Registry STABLE 69 (grep -c 'Analyzer,$' pkg/linters/registry.go = 69, no new linter this run). gh api repos/OWNER/REPO/issues?labels=sergo&state=all&per_page=100 --jq confirmed again for full reconcile (unpiped gh api works, raw jq binary would need approval). NEW GOTCHA: gh api repos/OWNER/REPO/issues/NNNN --jq '.state, .state_reason' reveals whether a closed issue was actually completed vs not_planned (auto-expiry) - a closed/not_planned issue with zero matching code change is a phantom-fix and a valid re-file target, not a duplicate. safeoutputs create_issue via direct --title/--body/--labels/--temporary_id bash flags CONFIRMED again this run: multi-paragraph single-quoted body, bold **Heading** (not ###), zero apostrophes, inline code spans with backticks all fine, no approval prompt. IMPORTANT CORRECTION: the newline-then-# path-validation guard also blocks safeoutputs create_discussion --body '...' bash-arg calls (tried ### headers per .claude/skills/reporting.md, got 'Newline followed by # inside a quoted argument can hide arguments from path validation') - this guard is NOT specific to create_issue, it applies to ALL bash-arg tool bodies in this sandbox. jq -Rs piping a file into create_discussion (the mcp-clis-recommended path for exactly this case) ALSO required approval and was not usable headlessly. Workaround used successfully: bold **Heading** instead of ### in the discussion body too - the reporting skill's ### preference is overridden by this sandbox constraint until a non-approval-gated way to feed a file-backed body is found.",
  "tool_change_R64": "STABLE 24 (no change from R63). activate_project reused OK (go/typescript/bash). Grep dedicated tool now confirmed to handle multiline mode (multiline:true, -B/-A) for cross-line AST-shape searches (for-range+append idiom) without approval - faster than reading whole files when hunting for a specific statement shape. gh api search/issues?q=repo:OWNER/REPO+TERM (not the -f q= form, which 404s) works for pre-file dedup checks on a brand-new linter name. safeoutputs create_issue via direct --title/--body/--labels/--temporary_id single-quoted-body Bash flags CONFIRMED again, zero apostrophes, and this run also used **bold** section headers plus a fenced code block (```go ... ```) inside the single-quoted body with NO leading # after any newline - worked fine in one shot, no approval prompt, no retry needed. NEW: found a project-level .claude/skills/reporting.md this run recommending ### headers for reports/discussions specifically (not issue bodies built via raw bash flags) - treat that skill file as authoritative for the discussion post; keep bold-not-### for safeoutputs create_issue bash-arg bodies since that guard is a sandbox command-parsing constraint, unrelated to GitHub markdown rendering.",
  "tool_change_R63": "STABLE 24 (no change from R62). activate_project reports active language servers go/typescript/bash again but get_diagnostics_for_file was NOT retested this run (relied on Read/Grep only, sufficient for source-level audit). serena CLI invocation confirmed: `serena activate_project --project <path>` works fine as a plain Bash call (no approval needed) once other jq/pipe ops are avoided. safeoutputs create_issue CONFIRMED again via direct --title/--body/--labels/--temporary_id flags as ONE Bash call with a single-quoted multi-line body: zero literal apostrophes (wrote out 'is not' style, used -- instead of em-dash contractions) AND bold **Heading** instead of ### (### still assumed rejected by path-validation guard, not re-tested since bold works). jq -Rs piping a file into safeoutputs (the mcp-clis-recommended heredoc+jq pattern) was ATTEMPTED FIRST and required interactive approval (blocked, no retry) - the direct single-quoted-flags method remains the only headless-reliable path in this sandbox for safeoutputs create_issue.",
  "registry_delta_history": "R58/R59: STABLE 42. R60:42->43 writebytestring(SuggestedFix). R61: registry moved from cmd/linters/main.go (multichecker.Main literal list) to pkg/linters/registry.go var allAnalyzers (main.go now just calls linters.All()); 43->67 (+24, undocumented gap). R62: STABLE 67. R63: 67->68, 68th=bufferresetbeforereuse (no SuggestedFix, pure diagnostic). R64: 68->69, 69th=slicemakezerolength (added 2026-09-11 PR #60310, no SuggestedFix, pure diagnostic). R65: STABLE 69. R66: 69->70, 70th=bufioscannererunchecked (added 2026-09-13, creation issue #60660, no SuggestedFix, pure diagnostic, not yet CI-enforced). R67: 70->71, 71st=blankassigncomma (added 2026-09-14 PR #60893, no SuggestedFix, pure diagnostic, not yet CI-enforced; found 14 prod sites all deliberate idiom, filed sg67a2). Detector: grep -c 'Analyzer,$' pkg/linters/registry.go (use Grep tool with output_mode:count, not raw Bash grep -c which needs approval for this pattern).",
  "tools": [
    "activate_project", "delete_memory", "edit_memory", "find_declaration",
    "find_implementations", "find_referencing_symbols", "find_symbol", "get_current_config",
    "get_diagnostics_for_file", "get_symbols_overview", "initial_instructions", "insert_after_symbol",
    "insert_before_symbol", "list_memories", "onboarding", "open_dashboard", "read_memory",
    "rename_memory", "rename_symbol", "replace_in_files", "replace_symbol_body",
    "safe_delete_symbol", "search_for_pattern", "write_memory"
  ],
  "params": "activate_project --project (NOT --path). find_symbol --name_path_pattern. find_referencing_symbols --name_path+--relative_path. search_for_pattern --substring_pattern. JSON stdin `.` sentinel.",
  "gotchas": "R62: jq/mkdir/cat/ls/grep/python3-with-args/go-build/gopls as raw Bash ALL need interactive approval and FAIL headlessly (confirmed again) - use Read/Write/Edit/Glob/Grep dedicated tools instead (approval-free); gh api and safeoutputs/serena CLI (with simple non-piped args) also work approval-free; python3 --version/node --version (bare, no script/args) succeed but running an actual script (python3 file.py) requires approval and fails. Bash also blocks writes/redirects outside the repo working directory even though the Write/Edit/Read tools CAN access /tmp/gh-aw/agent/ and /tmp/gh-aw/repo-memory/ freely - build payload files with Write, not shell redirection. safeoutputs create_issue: pass --title/--body/--labels/--temporary_id directly as bash args in a SINGLE safeoutputs invocation (no jq, no pipe) - body can be a multi-line single-quoted bash string (backticks/code-fences are literal and safe inside single quotes) but MUST NOT contain any literal apostrophe/single-quote char (breaks the quoting) and a newline immediately followed by '#' is rejected by a path-validation guard - use bold **Heading** instead of ### Heading in issue bodies built this way (reconfirmed R62). get_diagnostics_for_file/find_declaration/find_implementations need a live LSP (go/node) - NOT INSTALLED this run, degrade to Read/Grep. find_symbol broad 70s+ (prefer Grep) in past runs when LSP worked. `gh issue list --search` ERRORS 'malformed version' -> USE `gh api repos/OWNER/REPO/issues?labels=sergo&state=all&per_page=100 --jq` (gh api itself works fine unpiped/with --jq, unlike raw jq binary). RECONCILE gh api BEFORE strategy (grep CODE for fix, OPEN!=fixed). MEMORY 10KB TOTAL PATCH: EDIT files surgically + APPEND-ONLY strategies. This run safe-tools: create_issue(max 3)/create_discussion/missing_tool/missing_data/noop."
}
