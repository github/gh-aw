{
  "lastRun": "2026-09-22",
  "ruleCount": 62,
  "note": "index.ts registers rules under gh-aw-custom/<name>; 62 rule source files reconfirmed via `grep -l '= createRule(' eslint-factory/src/rules/*.ts` (excluding .test.ts) on 2026-09-22 - unchanged since 09-19. gh api \"repos/github/gh-aw/issues?state=open&per_page=100&labels=eslint-factory\" remains the ONLY authoritative dedup source - do not trust this file's issue list alone.",
  "thisRun_2026-09-22": "HEAD moved for the first time in 4 runs (2b90c3a, 'Add debug logging to under-instrumented add-command code paths #62535'), but `git show --stat 2b90c3a -- actions/setup/js` shows 880 files ALL as pure insertions (362896 insertions, 0 deletions/modifications) - this is the same shallow-clone squash-commit artifact seen at ff2eccd, not a real diffable change. Git-log-based 'new call sites' detection remains fundamentally unusable in this repo; stop treating any future HEAD movement as signal unless `git show --stat` shows a mix of modified (not just added) files. Pivoted entirely to the day-over-day open-issues snapshot diff (per 09-21 nextPriority): pulled fresh gh api snapshot, diffed against the 09-21 knownOpenIssuesSnapshot below. Exactly one item dropped off: #60757 (no-string-fallback-for-non-string-message alias FP on safeoutputs_cli.cjs). Re-read the rule source (unchanged) and the live file (unchanged shape, line numbers shifted 45-48->46-48) - grounding fully holds, this is only the 1st expiry of this theme, so refiled as a clearly-labeled recurrence with the same acceptance criteria. Also spot-checked the no-duplicate-constant-values string-min-group-size theory (#59647, expired 09-16, only 1 expiry so technically eligible) but the 09-18 run had ALREADY re-grepped and found zero live triggers before deciding not to refile - re-verifying that decision from scratch would just repeat the same zero-grounding grep, so respected the prior call and did not re-file (latent-only findings don't clear the filing bar regardless of expiry count). 1 issue filed today.",
  "rulesReviewedThisRun": [
    "no-string-fallback-for-non-string-message (re-verified recurrence of expired #60757, refiled)",
    "no-duplicate-constant-values (re-checked #59647 eligibility, respected prior 09-18 no-refile decision, not refiled)"
  ],
  "knownOpenIssuesSnapshot_2026-09-22": ["62317:require-invalid-date-check-before-compare arithmetic-derived-duration recurrence (still open)", "61543:require-spawnsync-error-check CHILD_PROCESS_OBJECTS alias gap (still open)", "61542:require-fetch-response-body-try-catch chain-unwrap gap (still open)", "61285:require-error-code-in-thrown-error ERROR_CODE_PATTERN vocabulary mismatch (still open)", "61284:require-error-code-in-thrown-error nested-alias-bypass (still open)", "61044:prefer-actions-exec-over-child-process shim.cjs dual-mode FP (still open)", "60757 EXPIRED 09-21 not_planned, unfixed - refiled today as new issue (see thisRun note)"],
  "nextPriorities": [
    "Git-log-based change detection is now confirmed DEAD across 5 consecutive runs (09-18 through 09-22, including one HEAD move at 2b90c3a that turned out to be another all-insertions shallow-clone artifact) - stop checking `git log` for this purpose entirely; only revisit if `git show --stat <newHEAD> -- actions/setup/js` ever shows non-zero deletions/modifications (a real diff), not just insertions.",
    "The highest-value recurring action each run remains: (1) re-pull the open-issues gh api snapshot, (2) diff against the prior day's knownOpenIssuesSnapshot for anything that expired, (3) for each freshly-expired item, re-read the rule+corpus and re-file ONLY if grounding still holds live AND this is not yet the 2nd+ expiry of that theme. This produced exactly 1 solid re-file today (no-string-fallback-for-non-string-message recurrence) - continue this as the first check every run.",
    "When an expired theme was PREVIOUSLY re-investigated and explicitly declined (e.g. #59647 on 09-18, found zero live grounding), do not re-run the identical grep just because it's newly eligible by expiry-count - only re-open it if something in the corpus has actually changed since that decision.",
    "The chronic try-catch-rule-utils.ts VariableDeclaration-suggestion gap (affects ~14 fs-sync/child-process-family rules, twice-filed-and-expired as #57868/#59891) still cannot be fixed via this workflow's tools (no create_pull_request safe-output) - keep flagging in the discussion report for a human/PR-capable workflow; do not attempt a 3rd filing.",
    "no-json-stringify-equality indirect-template-literal gap is likewise at 2 expiries (#57869/#59892) - same do-not-refile status, mention in report only.",
    "README doc debt still open (2/62 documented) - discussion-only mention, not worth another doc issue yet.",
    "6 of 7 issues from the 09-21 snapshot remain open with no fix uptake (61543/61542/61285/61284/61044 unchanged, 62317 is today's carried-forward recurrence) - if a future run finds ALL of these still open with zero expiries, that's a signal the daily cadence has fully exhausted this ruleset's easy findings; consider shifting to the source-fix-writeup angle for the 2+-expiry chronic themes instead of waiting on more expiries."
  ],
  "ops": [
    "repo-memory: Read/Write/Glob/Edit tools ONLY (Bash on /tmp blocked incl. mkdir/ls/find - use Glob with an explicit path for enumeration, NOT Bash find). Bash works fine for gh/CLI calls in the repo working dir. After Write, `safeoutputs push_repo_memory --memory_id default` to validate. TOTAL memory must be <=12KB (10KB patch limit) - keep lean, stub old findings files.",
    "create issue (WORKING RECIPE, confirmed 2026-09-21): jq --arg/$var gets falsely flagged by the sandbox's shell-expansion detector even inside single-quoted jq filters - AVOID entirely, bake literal strings into the jq filter. Also avoid heredoc bodies (`cat <<'EOF' > file` inside a single Bash call) - flagged as unstaticaly-analyzable file_redirect even with zero shell variables present; use the Write tool to create the body file instead, THEN run jq -Rs over it in a separate Bash call. Full recipe: (1) Write tool -> eslint-factory/.tmp_issueN_body.md (inside repo working dir only, NOT /tmp/gh-aw/*). (2) `jq -Rs '{title: \"literal text\", body: ., labels: [\"eslint\",\"eslint-factory\"]}' eslint-factory/.tmp_issueN_body.md > eslint-factory/.tmp_issueN.json`. (3) `safeoutputs create_issue . < eslint-factory/.tmp_issueN.json`. (4) rm both temp files, confirm `git status --short eslint-factory/` is clean.",
    "dedupe: gh api query-string form WORKS: `gh api \"repos/github/gh-aw/issues?state=open&per_page=100&labels=eslint-factory\" --jq '.[] | {number,title}'`. The -f flag form 403s.",
    "Shallow clone: git log/blame on eslint-factory/src/rules attributes ALL files to one recent squash commit - cannot determine true rule age this way. Use eslint.config.cjs/index.ts rule list diffed against this file's tracked rules[] instead. `git log --oneline -15 -- actions/setup/js` IS usable to detect whether HEAD has moved at all (even if it can't show granular history past the squash).",
    "eslint-factory: NO node_modules/dist, npm firewalled - no live lint. Ground statically via Grep on non-test .cjs (config ignores *.test.cjs) + rule src/tests. Rules registered gh-aw-custom/<name>; all warn except require-http-response-error-listener which is error.",
    "create_discussion params: title*, body*, category, integrity, secrecy.",
    "For a rule with 100+ importing/call-site files, the productive strategy is: read the rule's OWN helper functions for internal asymmetries first, THEN grep the corpus only for the specific shapes that asymmetry predicts.",
    "Cross-file same-name variables are IRRELEVANT to any bare-name-keyed rule's scope-blindness, since ESLint's create(context) runs fresh per file - only same-FILE name collisions across different scopes actually trigger a scope-blind bug.",
    "Small/rare-shape findings (e.g. a real logic gap with only 2-3 live call sites, all currently correct) do not meet the historical 'broadly applicable idiom' filing bar - document as a retained-but-not-filed theory in nextPriorities instead of forcing an issue.",
    "A rule's own .test.ts file is authoritative for whether a behavior is a known/intended limitation vs an undiscovered bug - always check the test file before filing an issue about a suspected gap, even if live-corpus grounding looks strong.",
    "Issues auto-expire (~1wk TTL) and get bot-closed as not_planned REGARDLESS of whether the underlying defect was actually fixed - a single expiry is NOT a signal of low priority, it's routine housekeeping. Re-file once on first expiry if grounding still holds live. Only stop re-filing (switch to describing a source fix in the discussion report instead) after 2+ expiries of the same theme with no fix uptake."
  ]
}
