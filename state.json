{
  "lastRun": "2026-09-24",
  "ruleCount": 62,
  "note": "index.ts registers rules under gh-aw-custom/<name>; 62 rule source files reconfirmed via `grep -l '= createRule(' eslint-factory/src/rules/*.ts` (excluding .test.ts) on 2026-09-23 - unchanged since 09-19. gh api \"repos/github/gh-aw/issues?state=open&per_page=100&labels=eslint-factory\" remains the ONLY authoritative dedup source - do not trust this file's issue list alone.",
  "thisRun_2026-09-23": "HEAD moved to 606cfab ('docs: unbloat repo assist example #62822') but `git show --stat 606cfab -- actions/setup/js` is yet another all-insertions shallow-clone squash artifact (881 files, 363664 insertions, 0 deletions) - git-log diffing conclusively dead, 6th consecutive confirmation, stop checking unless a real mixed-diff commit appears. Diffed 09-22 open-issues snapshot (7 items) vs today's (6 items): #61044 (prefer-actions-exec-over-child-process shim.cjs dual-mode FP) dropped off. Re-read the rule source expecting a routine expiry-recurrence refile per the standard playbook, but this one is DIFFERENT: the rule now contains a dedicated `requiresShimCjs()` check (create() returns {} early when the file requires ./shim.cjs) plus 2 new test cases (test.ts:47-48) explicitly covering both execFileSync/execSync-via-shim shapes. This is a genuine upstream FIX, not routine bot housekeeping - correctly did NOT refile. Also re-verified the require-http-response-error-listener addListener/`req.on(\"response\",cb)` idiom gap (filed 09-09, no longer in any open-issues snapshot) is likewise already fixed in source (LISTENER_METHODS includes addListener; getResponseEventCallback handles the response-idiom chain) with all 4 live corpus call sites (handle_agent_failure.cjs:1847, mcp_cli_bridge.cjs:248, start_mcp_gateway.cjs:560, mount_mcp_as_cli.cjs:245) correctly guarded. Spent remaining budget probing core-method-resolve.ts (shared by 7 no-core-*/setfailed rules) for a multi-hop alias-chain gap (isCoreAliasIdentifier's Variable branch only resolves ONE hop back to a direct `core`/JSDoc-param source, so `const a = core; const b = a;` would escape all 7 consumer rules) - zero live grounding (`const X = core;`-shape aliasing does not occur anywhere in the non-test corpus; the JSDoc DI pattern (7 files) is always used directly or via exactly one hop, never chained). Also reconfirmed both 2+-expiry chronic gaps (try-catch-rule-utils.ts SAFE_WRAPPABLE_STATEMENT_TYPES still ExpressionStatement/ReturnStatement-only; no-json-stringify-equality still requires direct JSON.stringify(...) operands, no indirect-variable resolution) are UNCHANGED in source - correctly not refiled a 3rd time. 0 issues filed today - a disciplined 0-issue run driven by two upstream fixes rather than lack of effort.",
  "rulesReviewedThisRun_2026-09-24": [
    "require-spawnsync-error-check (CONFIRMED #61543 FIXED: isSpawnSyncCall now calls shared isChildProcessObjectBinding(callee.object.name, callee.object, sourceCode) from try-catch-rule-utils.ts instead of a hardcoded CHILD_PROCESS_OBJECTS name Set - exactly resolves the filed gap. Issue is still OPEN in tracker; no close_issue/comment safe-output available to this workflow, flagged in discussion report for human follow-up.)",
    "require-fetch-response-body-try-catch (re-confirmed #61542 gap UNCHANGED: AwaitExpression visitor still requires the outer call's methodName to be literally json/text; a trailing .catch()/.then() on the body-read call still resolves methodName to catch/then and is skipped. Still open, correctly not re-filed.)",
    "require-invalid-date-check-before-compare (re-confirmed #62317 arithmetic-derived-duration gap UNCHANGED in source - BinaryExpression visitor still only inspects direct comparison operands, no propagation through arithmetic assignment. Still open.)",
    "no-string-fallback-for-non-string-message (re-confirmed #62560 gap UNCHANGED - alternateChainKey must textually equal messageChainKey minus '.message'; a nullish-coalescing/type-narrowed alias of the same catch binding still not resolved. Still open.)",
    "require-error-code-in-thrown-error (re-audited after #61285/#61284 both expired 2026-09-23T06:55 not_planned, 1st expiry each: messageReferencesErrorCode's BinaryExpression/TemplateLiteral nested-identifier branches (lines ~20-30) still check bare ERROR_CODE_PATTERN text match only, never resolveWriteOnceInitializerChain - #61284's gap is still real in source. ERROR_CODE_PATTERN still ERR_*/E[0-9]{3}-only, missing error_codes.cjs's own POLICY_FILE_PROTECTION_DENIED_REASON_CODE export - #61285's gap also still real. BUT grepped all consumers of POLICY_FILE_PROTECTION_DENIED_REASON_CODE (approve_workflow_run.cjs:372, push_to_pull_request_branch.cjs:529/1124) and all three uses are `reasonCode: POLICY_FILE_PROTECTION_DENIED_REASON_CODE` object-property assignments, never inside a `throw new Error(...)` message - zero live grounding, same as when originally filed 09-16. Per the no-duplicate-constant-values/no-json-stringify-equality precedent (ungrounded-at-filing themes are not refiled merely because they expired), did NOT refile either.)",
    "core-method-resolve.ts multi-hop alias theory - re-checked for `const \\w+ = core;`-shape bindings across corpus, still zero occurrences, theory remains ungrounded, not filed.",
    "detect_agent_errors.cjs (new/expanded file from 2026-09-23 PR #63048, Copilot custom-agent diagnostics) - reviewed both JSON.parse call sites (isUnsupportedModelToolsError, lines ~169 and ~182): both already inside enclosing try/catch with explicit catch blocks. fs.readFileSync at line ~534 also already try/catch-wrapped with getErrorMessage(err)+cause. No gap found in this new code."
  ],
  "knownOpenIssuesSnapshot_2026-09-24": ["62560:no-string-fallback-for-non-string-message alias-type-narrowing FP (recurrence of #60757, RE-CONFIRMED still-unfixed in source, still open)", "62317:require-invalid-date-check-before-compare arithmetic-derived-duration recurrence (RE-CONFIRMED still-unfixed in source, still open)", "61543:require-spawnsync-error-check CHILD_PROCESS_OBJECTS alias gap (CONFIRMED FIXED IN SOURCE via shared isChildProcessObjectBinding - tracker still shows open, needs human close)", "61542:require-fetch-response-body-try-catch chain-unwrap gap (RE-CONFIRMED still-unfixed in source, still open)", "61285/61284 EXPIRED 2026-09-23 not_planned - both re-audited, gaps still real in source but ZERO live grounding (only consumer of the one non-ERR_ error_codes.cjs export never appears inside a throw new Error message) - correctly not refiled, same as their original ungrounded filing basis"],
  "nextPriorities": [
    "PRIORITY: verify and close #61543 (require-spawnsync-error-check) - the CHILD_PROCESS_OBJECTS hardcoded-name-Set gap it describes has been fixed upstream (now uses the shared isChildProcessObjectBinding scope-resolved helper) but the issue is still open. This workflow has no close_issue/add_comment safe-output; a human or a PR-capable workflow needs to close it. This is the 2nd confirmed instance (after #61044) of a fix landing without the issue being closed - the ~1wk bot-auto-expiry is NOT catching genuine fixes reliably, only silence-based timeouts.",
    "Git-log-based change detection is DEAD across 7+ consecutive runs (09-18 through 09-24, latest e04de66/#63048) - stop checking `git log`/`git show --stat` for this purpose entirely; only revisit if a commit ever shows non-zero deletions/modifications in actions/setup/js, not just insertions. When a real feature PR lands (like #63048's Copilot diagnostics), instead grep the specific named new/changed files directly (e.g. detect_agent_errors.cjs, copilot_harness.cjs) for rule-relevant patterns rather than relying on the diff.",
    "core-method-resolve.ts multi-hop alias-chain theory (isCoreAliasIdentifier resolves only ONE hop back to a direct core/JSDoc-param source; `const a = core; const b = a;` escapes all 7 consumer rules) remains a real structural gap with zero live grounding after 2 dedicated checks (09-23, 09-24) - keep as documented latent theory, do not file without a live single-hop `const x = core;` pattern appearing first.",
    "The chronic try-catch-rule-utils.ts VariableDeclaration-suggestion gap (affects ~14 fs-sync/child-process-family rules, twice-filed-and-expired as #57868/#59891) still cannot be fixed via this workflow's tools (no create_pull_request safe-output) - keep flagging in the discussion report for a human/PR-capable workflow; do not attempt a 3rd filing.",
    "no-json-stringify-equality indirect-template-literal/indirect-variable gap is likewise at 2 expiries (#57869/#59892) - same do-not-refile status, mention in report only.",
    "require-error-code-in-thrown-error's two themes (#61285 pattern-vocabulary, #61284 nested-alias) are both real-in-source but ungrounded-in-corpus after 2 filing attempts (09-16 original, 09-24 re-check) - treat like the no-duplicate-constant-values/no-json-stringify-equality ungrounded-latent class: mention in report only, do not refile without new live grounding.",
    "README doc debt still open (2/62 documented) - discussion-only mention, not worth another doc issue yet.",
    "0 issues filed today (2026-09-24) - a disciplined 0-issue run. The 4 tracked open issues (62560,62317,61542, and 61543-pending-close) plus the newly-surfaced 61543-fix-needs-closing item are the whole actionable state; every other avenue investigated this run was ungrounded or already-fixed. Next run: check whether #61543 got closed, and keep watching for the next real (non-squash) feature PR to ground fresh findings against."
  ],
  "ops": [
    "repo-memory: Read/Write/Glob/Edit tools ONLY (Bash on /tmp blocked incl. mkdir/ls/find - use Glob with an explicit path for enumeration, NOT Bash find). Bash works fine for gh/CLI calls in the repo working dir. After Write, `safeoutputs push_repo_memory` to validate. TOTAL memory must be <=12KB (10KB patch limit) - keep lean, stub old findings files.",
    "create issue (WORKING RECIPE, confirmed 2026-09-21): jq --arg/$var gets falsely flagged by the sandbox's shell-expansion detector even inside single-quoted jq filters - AVOID entirely, bake literal strings into the jq filter. Also avoid heredoc bodies (`cat <<'EOF' > file` inside a single Bash call) - flagged as unstaticaly-analyzable file_redirect even with zero shell variables present; use the Write tool to create the body file instead, THEN run jq -Rs over it in a separate Bash call. Full recipe: (1) Write tool -> eslint-factory/.tmp_issueN_body.md (inside repo working dir only, NOT /tmp/gh-aw/*). (2) `jq -Rs '{title: \"literal text\", body: ., labels: [\"eslint\",\"eslint-factory\"]}' eslint-factory/.tmp_issueN_body.md > eslint-factory/.tmp_issueN.json`. (3) `safeoutputs create_issue . < eslint-factory/.tmp_issueN.json`. (4) rm both temp files, confirm `git status --short eslint-factory/` is clean.",
    "dedupe: gh api query-string form WORKS: `gh api \"repos/github/gh-aw/issues?state=open&per_page=100&labels=eslint-factory\" --jq '.[] | {number,title}'`. The -f flag form 403s. Also use `--jq 'length'` to get an exact count when the content output looks truncated.",
    "Shallow clone: git log/blame on eslint-factory/src/rules attributes ALL files to one recent squash commit - cannot determine true rule age this way. Use eslint.config.cjs/index.ts rule list diffed against this file's tracked rules[] instead.",
    "eslint-factory: NO node_modules/dist, npm firewalled - no live lint. Ground statically via Grep on non-test .cjs (config ignores *.test.cjs) + rule src/tests. Rules registered gh-aw-custom/<name>; all warn except require-http-response-error-listener which is error.",
    "create_discussion params: title*, body*, category, integrity, secrecy.",
    "For a rule with 100+ importing/call-site files, the productive strategy is: read the rule's OWN helper functions for internal asymmetries first, THEN grep the corpus only for the specific shapes that asymmetry predicts.",
    "Cross-file same-name variables are IRRELEVANT to any bare-name-keyed rule's scope-blindness, since ESLint's create(context) runs fresh per file - only same-FILE name collisions across different scopes actually trigger a scope-blind bug.",
    "Small/rare-shape findings (e.g. a real logic gap with only 2-3 live call sites, all currently correct) do not meet the historical 'broadly applicable idiom' filing bar - document as a retained-but-not-filed theory in nextPriorities instead of forcing an issue.",
    "A rule's own .test.ts file is authoritative for whether a behavior is a known/intended limitation vs an undiscovered bug - always check the test file before filing an issue about a suspected gap, even if live-corpus grounding looks strong.",
    "Issues auto-expire (~1wk TTL) and get bot-closed as not_planned - but as of 09-23, a single expiry is NOT reliably 'unfixed housekeeping': #61044 expired AND was genuinely fixed in the same window. ALWAYS re-read the current rule source (not just memory's cached description) before deciding to refile an expired item - the fix may have already landed.",
    "Bash sandbox flags multi-command chains, xargs, sort, printf-with-newlines, and some grep -E patterns as 'contains expansion'/'multiple operations requiring approval' even with zero actual shell variables - prefer the Grep/Glob/Read tools over Bash for exploratory searching; reserve Bash for single simple gh/git one-liners."
  ]
}
