---
"gh-aw": minor
---

Mark the `jira-create-issue`, `jira-update-issue`, `jira-add-comment`, and `jira-add-label` safe outputs as stable, and support temporary Jira issue IDs across same-run update, comment, and label operations.
